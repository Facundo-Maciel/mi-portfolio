import './style.css'

    const h2elements = document.getElementsByTagName('h2');
    const h2 = h2elements[0];
    console.dir(h2)

    // selecciona por clase
    // const pPorclase = document.querySelectorAll('.contenedor-tarjeta');
    // console.log(pPorclase)

/*     selecciona por id
    const tituloNav = document.getElementById('nav')
    console.log(tituloNav) */